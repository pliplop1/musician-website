// /src/main/java/com/docker/entity/TrackType.java
package com.docker.entity;

public enum TrackType {
    EMBED,      // Pour une piste intégrée (Spotify, SoundCloud...)
    UPLOADED_FILE // Pour un fichier audio envoyé (MP3, etc.)
}